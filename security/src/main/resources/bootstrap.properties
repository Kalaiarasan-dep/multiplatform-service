spring.application.name=omo-admin-api

#config server details
spring.cloud.config.label=main
spring.cloud.config.discovery.enabled=true
spring.cloud.config.discovery.serviceId=omo-config-server

#profile for testing local
spring.profiles.active=dev
# registry connection details
eureka.client.serverUrl.defaultZone=http://localhost:8761
eureka.client.instance.preferIpAddree=true
